# Optional site footer. Leave blank to render no footer.
# Store the complete footer HTML here. One-line HTML is safest, but quoted
# multi-line HTML is also supported by this repository's build scripts.
SITE_FOOTER_HTML=
